# One-time dismissal policy refinement receipt

The policy experiment was replayed in both modes. Dismissal eligibility changes
as intended, while explicit done remains terminal in either mode.

Validation: `20 passed`.

Decision: retain the default-resolves assumption for now; keep the alternate
mode as a reversible experiment until real behavioral evidence exists.

