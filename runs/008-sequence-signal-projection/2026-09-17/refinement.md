# Sequence signal projection refinement receipt

An untouched card now serves as a zero-activity comparison baseline while still
receiving its end-of-day `missed` outcome. Acted-on cards retain their raw
reorder/jump counts and terminal outcome.

Validation: `24 passed`.

