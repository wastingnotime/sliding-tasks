# Simulation slices

Each slice document records its implementation pack, runtime and architecture
mode, discovery boundary, use-case contract, business rules, ports,
deterministic tests, scenario, done criteria, exclusions, and refinement notes.

Slices extend one shared simulation environment. They are not separate sample
applications.

