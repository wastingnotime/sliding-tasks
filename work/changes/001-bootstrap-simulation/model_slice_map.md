# Model slice map

The first coherent slice is `today-decision-loop`, documented at
`sandboxes/simulation/docs/slices/001-today-decision-loop.md`.

Later candidates are sequence/reorder observation, metric usefulness over
longer histories, and released technology adapter contracts. These remain
unbuilt because the first slice can test the central Task → Card → Event model
without them.

