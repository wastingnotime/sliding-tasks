# Request: deepen the MRL simulation

Correct the shallow deterministic harness by introducing explicit application
use cases, runtime-visible actors, and independent repository-owned behavior
beams in one shared simulation environment.

