# Sliding Tasks MRL handoff — source record

- Source supplied by the user: `~/Downloads/sliding-tasks-mrl-handoff.md`
- Captured for change: `001-bootstrap-simulation`
- Source status: initial product/domain draft
- Requested phase: MRL simulation → experimentation → local materialization

## Governing sentence

> Turn recurring intentions into today's decisions, capture the resulting
> behavioral signals with minimal interaction, and use those signals to learn
> over time.

## Preserved source meaning

The handoff distinguishes Task (intention/rule), Card (dated occurrence), and
Event (historical fact). It defines a present-only board, factual event history,
regular and one-time semantics, deterministic day closure, basic analytics,
scenarios A-H, invariants, explicit non-goals, open questions, and a
materialization gate. The extracted semantic baseline and first slice retain
those constraints. The complete supplied source remains at the path above; this
record identifies its provenance without treating a Downloads file as runtime
input.

